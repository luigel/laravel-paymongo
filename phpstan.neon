includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    paths:
        - src
    excludePaths:
        # Local end-to-end sandbox; optional so clones without it still analyse.
        - e2e (?)
